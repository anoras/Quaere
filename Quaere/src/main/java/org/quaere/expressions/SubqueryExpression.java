package org.quaere.expressions;

public class SubqueryExpression extends QueryExpression {
    public SubqueryExpression(FromClause from, QueryBody queryBody) {
        super(from, queryBody);
    }
}
